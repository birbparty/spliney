# Root nim.cfg — applies to every module under this repo (Nim walks up the tree
# for nim.cfg), so the editor LSP resolves intra-package imports the same way
# nimble's build does, and editor + build agree on the memory model.
#
# Keep this in sync with srcDir in spliney.nimble.
--path:"src"
--mm:orc
